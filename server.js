const express=require("express"),http=require("http"),path=require("path"),UV=require("@titaniumnetwork-dev/ultraviolet"),app=express();const PORT=process.env.PORT||3000;app.use(express.static(path.join(__dirname,"public")));const uv=new UV();app.get("/service/:file",(req,res)=>{res.sendFile(path.join(__dirname,"public",req.params.file));});app.use((req,res)=>{uv.request(req,res);});app.listen(PORT,()=>console.log(`GooProxy UV running on http://localhost:${PORT}`));
